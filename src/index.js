import React from 'react';
import ReactDOM from 'react-dom';
import Design from './view/DesignSystem/Design';
import './index.css'

ReactDOM.render(
  <React.StrictMode>
    <Design />
  </React.StrictMode>,
  document.getElementById('root')
);